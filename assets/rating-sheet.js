(()=>{'use strict';function init(){if(document.getElementById('waslhaRatingSheet'))return;const s=document.createElement('div');s.id='waslhaRatingSheet';s.className='waslha-rating-sheet hidden';s.innerHTML=`<div class="rating-sheet-backdrop"></div><section class="rating-sheet-card" role="dialog" aria-modal="true" aria-labelledby="waslhaRatingTitle"><button class="rating-sheet-close" type="button" aria-label="إغلاق">×</button><span class="eyebrow">اكتملت الرحلة ✓</span><h2 id="waslhaRatingTitle">كيف كانت رحلتك؟</h2><p>قيّم الكابتن حتى نعرف رأيك ونحسّن الخدمة.</p><div class="waslha-rating-stars" dir="ltr">${[1,2,3,4,5].map(n=>`<button type="button" data-rating="${n}" aria-label="${n} نجوم">★</button>`).join('')}</div><textarea maxlength="500" placeholder="اكتب ملاحظتك، اختياري..."></textarea><div class="rating-sheet-error"></div><button class="rating-submit" type="button">إرسال التقييم</button><button class="rating-later" type="button">ليس الآن</button></section></div>`;document.body.appendChild(s);let score=0,rideId=null;const hide=()=>s.classList.add('hidden');const show=ride=>{rideId=ride?._id||ride?.id;if(!rideId)return;score=0;s.querySelectorAll('[data-rating]').forEach(x=>x.classList.remove('selected'));s.querySelector('textarea').value='';s.querySelector('.rating-sheet-error').textContent='';s.classList.remove('hidden')};s.querySelectorAll('[data-rating]').forEach(x=>x.onclick=()=>{score=Number(x.dataset.rating);s.querySelectorAll('[data-rating]').forEach(y=>y.classList.toggle('selected',Number(y.dataset.rating)<=score))});s.querySelector('.rating-sheet-backdrop').onclick=hide;s.querySelector('.rating-sheet-close').onclick=hide;s.querySelector('.rating-later').onclick=hide;s.querySelector('.rating-submit').onclick=async()=>{const e=s.querySelector('.rating-sheet-error'),b=s.querySelector('.rating-submit');if(!score){e.textContent='اختر تقييماً من 1 إلى 5 نجوم.';return}try{b.disabled=true;b.textContent='جارٍ الإرسال...';await api('/ratings/rides/'+encodeURIComponent(rideId),{method:'POST',body:JSON.stringify({score,comment:s.querySelector('textarea').value.trim()})});localStorage.removeItem('waslha_rating_pending_'+rideId);hide();toast('شكراً لك، تم تسجيل تقييمك ⭐')}catch(err){e.textContent=err.message||'تعذر إرسال التقييم.'}finally{b.disabled=false;b.textContent='إرسال التقييم'}};window.WaslhaRating={show,hide}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();
